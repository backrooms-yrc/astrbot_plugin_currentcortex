# 更新日志 (Changelog)

本项目所有显著变更均记录于此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),版本号遵循语义化版本;各版本日期为对应版本号写入 `metadata.yaml` 的提交日期。**v2.0.0 之前的版本从未正式发布(无 tag/Release),历史条目系从提交记录回溯整理,粗略概括以近详远略为原则。**

## [Unreleased]

## [2.2.1] - 2026-08-24

### 变更

- **中转服务器「一键部署 / 卸载 / 暴露公网」安全加固**（响应插件市场安全审核）：一键部署改为**两步确认**——先在确认面板完整披露将执行的系统级操作（缺失时安装 Bun 官方脚本、克隆 DG-LAB 官方仓库、创建 systemd 服务），再**键入版本号**（如 `v3`）确认才执行；后端对请求体 `confirm` 字段做一致性校验，绕过前端直接调用接口一律 400 拒绝。卸载与暴露公网（中转 / WebUI）接口同样要求前端二次确认标记（`confirm=true`）。README 补充「安全披露」段落：全部 Page API 经框架层管理员鉴权（`require_plugin_scope`，未登录管理员无法触达）、内容来源固定（Bun 官方安装脚本 + `dungeonlab-open/dglab-websocket-server` 官方仓库，浅克隆）、部署本身不修改防火墙（公网暴露由独立开关另行确认）、卸载一键完全回滚、无定时任务/无信息收集

## [2.2.0] - 2026-08-21

### 新增

- **跨群记忆 LLM 摘要**：新增配置 `cross_group_summary_enable`（默认 `false`）、`cross_group_summary_threshold`（默认 `20`，slider 5~100）、`cross_group_summary_provider_id`（专用模型，留空复用当前会话模型）。注入 LLM 的记录条数超过阈值时，先调 LLM 把原始记录压缩成一段话题摘要（「最近大家在聊 XX、YY」）再注入正文，而非堆砌几十条原始消息——省 token，也让回复更像「记得住重点」；摘要失败（无 provider/超时/返回空）自动降级为原始记录注入，同一份记忆 5 分钟内复用摘要缓存，避免频繁调用
- **按群聊开关 scope 分级**：`/开关` 命令新增可选功能域参数，支持只关某一类功能（如 `/开关 off media 2h` 只关媒体解析、`/开关 off memory` 只关跨群记忆），不再一刀切关闭全部命令；`GroupSwitchStore` 存储 key 从纯 `umo` 升级为 `umo|scope`（全局禁用仍为纯 `umo`，旧格式加载后自动视为全局禁用，无需迁移）；`/开关列表` 同步展示功能域与恢复时间。**限制**：功能域开关只约束用户命令，AI 通过 LLM 工具自主调用（图片/点歌/电击）不经过命令入口、不受域级开关影响（全局关闭仍拦截），README 已注明
- **媒体解析失败原因分级**：新增 `MediaParserError.kind` 分类（链接过期 / 内容已删除或私密 / 平台反爬拦截 / 网络超时 / 网络异常 / 接口异常 / 格式不识别），解析失败时按原因给出精确中文提示并附截断后的原始信息，不再甩一坨原始异常文本
- **媒体解析结果缓存**：新增内存 LRU 缓存（url → 解析结果，TTL 默认 600 秒），命中直接返回，减少对小红书/B站/抖音/微博的请求频率、降低触发反爬/封 IP 概率；仅缓存成功结果，失败实时重试。配置 `media_parse_cache_enable`（默认 `true`）、`media_parse_cache_ttl`（默认 `600`，slider 60~3600）
- **媒体解析批量链接**：`/解析` 支持一条消息粘贴多个链接，去重保序后依次解析并逐条回复（单次上限 5 条，防刷屏与平台限流），`_parse_media_url` 拆分为 `_parse_media_urls`
- **媒体解析联动跨群记忆**：解析成功后把「谁解析了什么链接」写入跨群记忆（`tag=media`），后续对话机器人可自然回溯「你刚才发的那个视频」，正式接线 v2.1.0 预留的话题标签 API；记忆为增强项，任何失败静默，不影响解析主流程
- **群开关静默期仅提醒一次**：关闭期间第一次收到本插件指令时回复一句简短提示（「本群已关闭机器人，管理员可发 /开关 on 恢复」），同一群同一功能 1 小时内仅提醒一次，避免新用户 @ 机器人没反应摸不着头脑。配置 `group_switch_hint_enable`（默认 `true`）
- **测试**：`test_memory_and_switch.py` 新增 8 项 scope/批量回归用例（store 域级语义与复合 key 加载、域参数解析、时长解析跳过域词、守卫域检测、`/开关 on <域>` 先判后操作、开关列表域标签、批量提取与上限提示计数），共 33 项

### 变更

- **命令总览更新**：`/cc`（别名 `/帮助` `/help` `/菜单`）总入口的命令总览数据与媒体解析帮助文本同步纳入本次新增能力（scope 分级开关、批量解析、失败原因分级、结果缓存），媒体解析帮助文本补充批量与失败分级用法

### 修复（合并前维护者修正）

- `/解析` 批量上限提示的计数错误：截断后再取 `len(urls)`，导致提示恒为「检测到 5 条以上链接」，实际条数丢失；改为截断前保存原始条数
- `/开关 on <功能域>` 在「全局关闭 + 该域被单独关闭」时先删条目再判断：`set_enabled` 会先移除该域条目（用户设置的限时禁用被静默清除），随后又提示「并未被单独关闭」（与事实相反）；改为先判断后操作——域未被单独关闭时仅提示、零状态变更；域确被单独关闭时按指令恢复并如实提示「全局仍处于关闭状态」。`GroupSwitchStore` 新增 `has_disabled_entry()` 支持该判定

### 文档

- README 四语言版本（简中/EN/JA/繁中）补齐本版本全部内容：功能域开关用法与配置、解析增强（失败分级/缓存/记忆联动）、LLM 摘要配置、`/帮助` 命令别名，并注明 LLM 工具不受域级开关限制的边界

## [2.1.0] - 2026-08-21

### 新增

- **跨群聊记忆时效过滤与关键词清理**：新增配置项 `cross_group_max_age_hours`（默认 `0` = 不限时效，仅按条数裁剪，保持旧行为；建议 12~48），注入 LLM 时只保留该时效内的记录；新增指令 `/忘记 <关键词>`（别名 `/forget_memory` `/忘记记忆`，仅管理员），按关键词删除本平台记忆中的匹配记录（子串匹配、不区分大小写），无需整体清空。记录结构升级为 `{ts, tag, content}`，加载时自动兼容迁移旧纯字符串格式（旧记录 `ts=0.0`，启用时效过滤时不参与注入）
- **按群聊开关：限时关闭 + 可视化列表**：`/开关 off` 支持可选时长（如 `/开关 off 2h`、`/开关 off 2小时30分钟`，单位 s/秒、m/分/分钟、h/时/小时、d/天，可组合），到期懒惰过期自动恢复，重启后倒计时仍有效；`/开关 status` 显示剩余恢复时间；新增 `/开关列表` 指令（别名 `/switch_list` `/开关状态列表`，仅管理员），按当前平台列出所有被关闭的群及恢复时间。存储格式升级为 `{umo: until}` 字典，旧 `{"disabled": [...]}` 列表格式自动迁移（一律视为永久禁用）
- **媒体解析新增微博**：新增指令 `/weibo`（别名 `/微博`）并接入 `/解析` 自动识别，支持 `weibo.com/<uid>/<bid>`、`m.weibo.cn/detail|status/<id>`、`weibo.cn/status/<id>`、`t.cn` 短链；优先走 m.weibo.cn 移动端接口，失败自动降级网页 og 标签兜底；返回正文、作者、转发/评论/点赞数、配图（最多 9 张）与视频链接（如有）；`/解析 help` 帮助文本同步更新
- **测试**：新增 `test_memory_and_switch.py`（25 项回归：两个存储模块的格式迁移与全公开方法、开关/忘记命令的参数解析与守卫白名单），登记 `.gitignore` 测试白名单

### 修复

- `/开关列表` 在已关闭的群内会被开关守卫静默拦截：`_is_switch_command` 放行白名单此前仅含 `开关/toggle/switch`，已加入 `开关列表/switch_list/开关状态列表`
- `/忘记记忆` 别名解析出错误关键词：前缀剥离正则备选顺序导致「忘记」恒先于「忘记记忆」命中，`/忘记记忆 关键词` 实际搜索「记忆 关键词」，已按最长优先修正
- `/开关列表` 未按平台过滤：store 为跨平台全局存储，此前会列出其他平台（如 Telegram）的条目，现按当前会话 umo 平台段过滤，并以群号代替原始 umo 展示
- `/开关 off <时长>` 无法识别时（如 `2x`）静默按永久禁用处理：现返回错误提示并保持原状态；同时支持复合时长求和（如 `2小时30分钟` = 2.5 小时），总时长 ≤ 0 视为无效

### 说明

- `CrossGroupMemoryStore.record(tag=...)` / `get_recent(tag=...)` 为存储层预留的话题标签 API，本版本未接线到任何指令或配置，无用户可见行为变化

## [2.0.11] - 2026-08-20

### 文档

- README 多语言支持扩展：新增繁体中文文档 `README_ZH-TW.md`（全量翻译并按台湾用语习惯本地化，指令字面量保持简体原文），语言切换入口扩展为 简体中文 / English / 日本語 / 繁體中文，项目结构树同步收录

## [2.0.10] - 2026-08-20

### 文档

- README（中文）视觉优化：页首改为居中 Hero 头部（logo 主视觉 + Release/License/Python/AstrBot/Stars/QQ 徽章栏 + 快捷链接 + 国旗式语言切换），置顶公告标题压缩为粗体行，文末改为居中收尾块（版本/更新日志/问题反馈/协议 + Star 号召）；内容与章节锚点零改动

## [2.0.9] - 2026-08-20

### 文档

- README 多语言支持扩展：新增日文文档 `README_JA.md`（全量翻译），语言切换入口扩展为 中文 / English / 日本語，项目结构树同步收录

## [2.0.8] - 2026-08-20

### 文档

- **README 多语言支持**：新增英文文档 `README_EN.md`（全量翻译，与中文版结构、表格、命令示例一一对应），中文/英文 README 顶部互设语言切换入口，项目结构树同步收录

## [2.0.7] - 2026-08-20

### 变更

- 配置项 `llm_tools_enable`（LLM 工具总开关）默认值由 `false` 改为 `true`：未显式配置过的用户升级后即启用 LLM 工具（AI 自主调用图片/点歌/电击等），已显式设置的用户不受影响；电击控制涉及物理设备，不放心可在配置面板关闭

### 修复

- 总 Pages → 郊狼控制「中转服务器一键部署」在精简环境（systemd/容器启动 AstrBot、无 `HOME` 变量）下必失败：bun 官方安装脚本以 `set -u` 运行并引用 `$HOME`，报 `HOME: unbound variable` 中断安装（现为部署子进程显式注入 `HOME`/`USER`/`BUN_INSTALL` 并把 bun 目录加入 `PATH`，依赖安装改用解析出的 bun 绝对路径）

## [2.0.6] - 2026-08-17

### 修复

- 清理漫画内容功能的残留引用：`.gitignore` 中已删除测试文件的白名单条目、Issue 模板中已下线功能的模块选项

## [2.0.5] - 2026-08-17

### 文档

- README 在原漫画内容章节位置补充该功能已于 v2.0.4 移除的说明
- 更新日志移除条目的表述调整

## [2.0.4] - 2026-08-17

### 移除

- **移除漫画内容功能**：对应命令与别名、API 客户端、2 个配置项、测试文件及 README/帮助文档全部下线，后续版本不再提供（移除前完整快照已本地备份）

## [2.0.3] - 2026-08-17

### 修复

- CCDG WebUI 内置文档「连接方式」补齐具体配对操作步骤（对照 [DG-LAB 官方 Socket 文档](https://github.com/DG-LAB-OPENSOURCE/DG-LAB-OPENSOURCE/blob/a48a1701ea/socket/%E5%BF%AB%E9%80%9F%E4%BD%93%E9%AA%8C.md)）：新增「App 版本选择」（推荐下载 DG-LAB 4，<https://dungeon-lab.cn/>，3.x 与 4.x 不能互连）；v3 App 步骤（主界面 SOCKET → 连接服务器 → 相机图标扫码）；v4 App 步骤（Socket控制 → 创建Socket控制 → 扫描二维码）

## [2.0.2] - 2026-08-17

### 修复

- CCDG WebUI「暴露公网」开关只改监听地址、不放行防火墙：打开后公网实际无法访问（现复用中转暴露开关逻辑，打开时自动 `ufw allow` 端口并以 `ufw status` 实际查询校验生效，失败不落配置显式报错；关闭/停用 WebUI 时自动收回放行，删除失败在回执中提示）

## [2.0.1] - 2026-08-17

### 新增

- **总 Pages → 郊狼控制：中转服务器一键部署（v3/v4）**
  - v3（9999）/ v4（9998）独立检测与部署：自动安装 Bun、克隆官方仓库、写入配置、创建 systemd 常驻服务并做协议自检，全程约 10~60 秒；历史手工部署可被识别接管
  - 暴露公网开关（默认关闭）：官方服务端监听固定全接口，可达性由防火墙控制——关闭仅本机可达（显示 `ws://127.0.0.1:端口`），开启自动 ufw 放行并探测显示公网地址，关闭自动收回，放行前二次确认
  - 一键卸载：停服务、删 systemd 单元、收回放行；源码目录保留，重装秒级

### 修复

- 中转「暴露公网」开关打开后仍显示未暴露：后端 `ufw allow` 失败被静默吞掉仍返回成功（现强制以 `ufw status` 实际查询校验规则生效，失败显式报错并透传原因）
- `mdui-switch` 受控组件失同步：操作失败/取消确认时开关视觉卡在"已点击"状态（现处理起点先将 DOM 回同步到数据值；CCDG WebUI 的总开关与公网暴露开关一并修复同款隐患）
- CCDG WebUI 内置文档「连接方式」表述修正（对照 [DG-LAB 开源蓝牙协议仓库](https://github.com/DG-LAB-OPENSOURCE/DG-LAB-OPENSOURCE) 与本插件实际实现）：重写为中转服务器 + 二维码配对架构，拆分为 **v3 APP（DG-LAB 3.0 App，端口 9999、根路径裸连、旧版二维码）** 与 **v4 APP（DG-LAB 4.0 App，端口 9998、可带路径前缀、新版二维码仅 4 App 可识别）** 两节；修正 `/dglab bind` 说明与 FAQ 排障（clientId 超时 / HTTP 404 / v4 扫码）；删除无出处参数（BLE 4.2+、脉冲宽度 50-500μs），波形频率修正为「设备端 10-240，App 输入 10-1000 换算下发」

## [2.0.0] - 2026-08-15

首个正式发布版本(首个 tag 与 Release)。⚠️ 大版本:DG-LAB 协议栈完全重写。

### 新增

- **DG-LAB V3 / V4 协议全支持,连接时自动识别**(`bind` → V3 / `hello` → V4),老用户无需改配置;存量旧 v2 中转服务器保持兼容(修复 [#3](https://github.com/backrooms-yrc/astrbot_plugin_currentcortex/issues/3))
- V4 模式:`hello` 握手 + 透传 RPC(`devices.get` 设备发现 / `device.op` 强度与波形任务 / `device.op.clear` 清理),新版二维码 `dungeon-lab.cn/s/?v=1&action=socket&url=...`(需 DG-LAB 4 APP 扫码);支持路径前缀(如 `/v4`)自动回退识别
- V4 绝对强度智能换算:按 APP 回传的当前强度换算增量,无回传时退化为临时强度任务;V4 设备事件内部合成为 V3 风格回调,连接池/WebUI/命令层零改动
- 配置项 `dglab_protocol`(`auto` / `v3` / `v4`,默认 `auto`),设备绑定记录持久化协议版本
- 测试 `test_dglab_protocol.py`:36 个用例,内置按官方 v3/v4 服务端行为实现的本地 mock,端到端覆盖全链路
- Issue 模板(Bug 反馈 / 功能建议表单)、PR 模板(自测清单)、[贡献指南](CONTRIBUTING.md)
- 本更新日志 `CHANGELOG.md`

### 修复

- V3 服务端下 `error` 帧被误判为绑定成功(旧日志 `via error` 的根因,即 #3 中「等待服务器分配 clientId 超时」)
- 多步强度增减在 V3 语义下会把「减少 N」误执行为「设置为 0」(V3 中 `type:4` 不再透传原始指令,改为多条 `type:1/2` 报文)
- 通道清理指令缺 `channel` 字段导致 V3 下清理错通道(V3 依据 channel 而非 message)
- `test_dglab_integration.py` 过期的模块 stub 与 API 断言(53/53 通过)

### 变更

- README:DG-LAB 章节重写(V3/V4 说明、Bun 部署指引、#3 症状排障)

### 移除

- **Qtine 兼容包**(`qtine/` 目录):Qtine 2 已转向 C 实现,旧兼容包停止维护

## [1.10.0] - 2026-08-15

> 未单独发布,版本号随即升为 v2.0.0,内容已并入上方 [2.0.0]。

## [1.9.1] - 2026-08-13

- 修复 README 中 DG-LAB 中转服务器链接误指蓝牙协议仓库
- 语义分段回复系列修复:LLM 模式超时降级、事件循环冻结 168-215 秒、分段 provider 误用推理模型(根因修复)——注:V3/V4 协议适配工作自本版本线开始,最终随 v2.0.0 发布

## [1.9.0] - 2026-08-10

- 新增插件总 Pages(AstrBot WebUI 集成面板:仪表板 / 帮助中心 / 可视化设置 / 郊狼控制 / 联系我们,mdui 2 风格)
- 语义分段:LLM 超时降级修复、降级时 punct 切分过碎修复、分段数量上限 slider 扩到 99

## [1.8.0] ~ [1.8.5] - 2026-08-10

- 新增 `/帮助` 命令,渲染全部命令分类总览为图片;入口后改为 `/cc` 避免与其他插件冲突
- `/cc` 图片模板多轮视觉重设计(晴空蓝 + 雪雾白、SVG 图标),并修复模糊问题(PNG 2x 高清渲染)
- 修复 Qtine 兼容层 WebUI 默认绑定 `0.0.0.0` 的安全问题

## [1.7.0] ~ [1.7.5] - 2026-08-09/10

- QQ 群宣传功能:群号改为内置常量、移除全部开关;大模型回复不再注入群号水印;图片获取按 10% 概率追加群号
- 修复私聊时分段回复首段错误 @ 并引用用户的问题(仅群聊保留)

## [1.6.0] - 2026-08-09

- **LLM 语义分段回复**:按语义完整性智能切分,默认模式改为 llm,首条消息 @ 并引用回复用户
- 注册 **LLM 工具**(function calling):图片获取 / 点歌 / 电击控制可被 AI 自主调用

## [1.5.7] - 2026-08-02

- 点歌新增酷狗音源与 `/音源` 切换(auto 自动路由)

## [1.5.6] - 2026-08-01

- 点歌命令增加每会话去重与冷却,防止连点并发过载

## [1.5.5] - 2026-07-31

- 点歌文件模式超限时自动转码 MP3,修复 QQ 大文件发送失败

## [1.5.4] - 2026-07-31

- 新增可配置的**分段回复**功能(punct/length 模式),默认切分符号加入中英文逗号,支持多字符切分词

## [1.5.3] - 2026-07-31

- **WebUI 安全加固**:修复默认公网暴露 / 上传 / 路径穿越 / 注册 / XSS 等问题,WebUI 默认关闭

## [1.5.2] - 2026-07-31

- 修复 WebUI 聊天文件接口的路径穿越漏洞;README 置顶交流群信息

## [1.5.1] - 2026-07-31

- 新增**按群聊独立开关**插件全部命令(resolve [#2](https://github.com/backrooms-yrc/astrbot_plugin_currentcortex/issues/2))

## [1.5.0] - 2026-07-30

- DG-LAB **多设备支持**(绑定/广场/波形修复),LeiZ API key 统一
- 修复 Pixiv 随机图固定与搜索失效;补充跨群聊记忆

## [1.4.0] / [1.4.3] - 2026-07-22/25

- DG-LAB 多设备支持与 API key 统一的前置重构;点歌支持原声文件下载
- 新增 Qtine 兼容层(PR #1);新增插件 logo

## [1.3.0] - 2026-06-13

- DG-LAB Web 功能增强(投稿广场 / 邮箱 / Turnstile 等配套存储)

## [1.2.0] - 2026-05-16 ~ 05-31

- **集成 DG-LAB 设备管理**:Socket 客户端、连接池、设备存储、命令处理器、波形预设、中文命令别名
- 新增 DG-LAB WebUI 控制面板(Material Design 3 风格)与用户/权限管理体系
- 点歌新增直发语音条模式

## [1.1.0] - 2026-05-05 ~ 05-14

- 新增**男娘图片**功能(含 API 密钥认证)
- 新增**网易云音乐点歌**(QQ 语音条支持、音频下载与消息处理重构)

## [1.0.0] - 2026-05-01/02

- 初始版本:**Pixiv 随机图片**(参数筛选)、**每日一言**、**天气查询**、命令解析器与配置管理

---

版本对比:[v1.9.1...v2.0.0](https://github.com/backrooms-yrc/astrbot_plugin_currentcortex/compare/v1.9.1...v2.0.0)
